# AI for Learning Report LaTeX Project

This is the working ACM-format report project for DeepDive.

## Files
- `main.tex` — main paper
- `references.bib` — bibliography entries currently grounded in confirmed project sources
- `.gitignore` — common LaTeX ignores

## Notes
- Figure assets are not added yet; `main.tex` currently uses placeholder boxes so the paper can compile in Overleaf immediately.
- Replace placeholder figures with exported assets later from `../../assets/` or copy them into this repo.
